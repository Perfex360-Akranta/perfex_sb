spring.application.name=perfex_sb
server.port=9090
spring.datasource.url=jdbc:postgresql://103.39.132.39:5432/PERFEXPGSBDEV
spring.datasource.username=campuser
spring.datasource.password=campusr_!234

 spring.jpa.hibernate.ddl-auto=none
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# HikariCP connection pool settings
spring.datasource.hikari.maximum-pool-size=20    
spring.datasource.hikari.minimum-idle=5           
spring.datasource.hikari.idle-timeout=30000      
spring.datasource.hikari.connection-timeout=20000 
spring.datasource.hikari.max-lifetime=1800000  

# spring.jpa.hibernate.ddl-auto=none
 spring.jpa.show-sql=false
 spring.jpa.properties.hibernate.format_sql=true

# spring.jpa.open-in-view=false
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

spring.jpa.properties.hibernate.jdbc.fetch_size=1000
spring.jpa.properties.hibernate.default_batch_fetch_size=1000
logging.level.org.springframework.security=DEBUG

spring.config.import=file:./config/perfex-terminal-secrets.properties

# ============================================================
# TERMINAL RECOVERY ENCRYPTION
# ============================================================

#perfex.terminal-recovery.crypto.active-key-id=${PERFEX_TERMINAL_RECOVERY_ACTIVE_KEY_ID}
#perfex.terminal-recovery.crypto.key-ring=${PERFEX_TERMINAL_RECOVERY_KEY_RING}

# ============================================================
# TERMINAL BROWSER COOKIE
# ============================================================

perfex.terminal.cookie.name=${PERFEX_TERMINAL_COOKIE_NAME:PERFEX_TERMINAL}
perfex.terminal.cookie.secure=${PERFEX_TERMINAL_COOKIE_SECURE:false}
perfex.terminal.cookie.same-site=${PERFEX_TERMINAL_COOKIE_SAME_SITE:Lax}
perfex.terminal.cookie.max-age-days=${PERFEX_TERMINAL_COOKIE_MAX_AGE_DAYS:365}

#File Upload Configurations
spring.servlet.multipart.max-file-size=2GB
spring.servlet.multipart.max-request-size=2GB
document.storage.path=D:/PerfexFiles




